import { defineConfig, globalIgnores } from 'eslint/config';
import nextVitals from 'eslint-config-next/core-web-vitals';
import nextTs from 'eslint-config-next/typescript';

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    settings: {
      // Explicitly set the React version so eslint-plugin-react doesn't try to
      // auto-detect it via the removed context.getFilename() API in ESLint 10.
      react: { version: '19' },
    },
  },
  globalIgnores([
    // Next.js / build artefacts
    '.next/**',
    'out/**',
    'build/**',
    'next-env.d.ts',
    // Generated by fumadocs-mdx at build/dev time — not source code
    '.source/**',
  ]),
]);

export default eslintConfig;
