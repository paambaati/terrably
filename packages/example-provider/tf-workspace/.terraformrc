provider_installation {
  dev_overrides {
    # Points Terraform at the SEA binary in bin/ after running: pnpm build:sea
    # Adjust the path if your project root is different.
    "registry.terraform.io/example/example" = "${path.cwd}/../bin"
  }
  direct {}
}
