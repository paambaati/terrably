terraform {
  required_providers {
    {{ prefix }} = { source = "myorg/{{ prefix }}" }
  }
}

provider "{{ prefix }}" {
  # api_url = "http://127.0.0.1:8765"
}

resource "{{ prefix }}_item" "example" {
  name = "hello"
}

output "item_id" { value = {{ prefix }}_item.example.id }
