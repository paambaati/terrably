// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v3.19.1
// source: grpc_stdio.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Empty } from "./grpc_controller";

export const protobufPackage = "plugin";

/**
 * go-plugin GRPCStdio service – required to avoid "method not found" errors.
 * The host expects this to be present; we serve it but return nothing.
 */

export interface StdioData {
  channel: StdioData_Channel;
  data: Uint8Array;
}

export enum StdioData_Channel {
  INVALID = 0,
  STDOUT = 1,
  STDERR = 2,
  UNRECOGNIZED = -1,
}

export function stdioData_ChannelFromJSON(object: any): StdioData_Channel {
  switch (object) {
    case 0:
    case "INVALID":
      return StdioData_Channel.INVALID;
    case 1:
    case "STDOUT":
      return StdioData_Channel.STDOUT;
    case 2:
    case "STDERR":
      return StdioData_Channel.STDERR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return StdioData_Channel.UNRECOGNIZED;
  }
}

export function stdioData_ChannelToJSON(object: StdioData_Channel): string {
  switch (object) {
    case StdioData_Channel.INVALID:
      return "INVALID";
    case StdioData_Channel.STDOUT:
      return "STDOUT";
    case StdioData_Channel.STDERR:
      return "STDERR";
    case StdioData_Channel.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseStdioData(): StdioData {
  return { channel: 0, data: new Uint8Array(0) };
}

export const StdioData: MessageFns<StdioData> = {
  encode(message: StdioData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channel !== 0) {
      writer.uint32(8).int32(message.channel);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StdioData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStdioData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.channel = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StdioData {
    return {
      channel: isSet(object.channel) ? stdioData_ChannelFromJSON(object.channel) : 0,
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: StdioData): unknown {
    const obj: any = {};
    if (message.channel !== 0) {
      obj.channel = stdioData_ChannelToJSON(message.channel);
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StdioData>, I>>(base?: I): StdioData {
    return StdioData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StdioData>, I>>(object: I): StdioData {
    const message = createBaseStdioData();
    message.channel = object.channel ?? 0;
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

export interface GRPCStdioServiceImplementation<CallContextExt = {}> {
  /**
   * StreamStdio returns a stream that the plugin uses to send stdout/stderr
   * data back to the host. The host sends an initial empty request.
   */
  streamStdio(
    request: Empty,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<StdioData>>;
}

export interface GRPCStdioClient<CallOptionsExt = {}> {
  /**
   * StreamStdio returns a stream that the plugin uses to send stdout/stderr
   * data back to the host. The host sends an initial empty request.
   */
  streamStdio(request: DeepPartial<Empty>, options?: CallOptions & CallOptionsExt): AsyncIterable<StdioData>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type ServerStreamingMethodResult<Response> = { [Symbol.asyncIterator](): AsyncIterator<Response, void> };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
