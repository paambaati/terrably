terraform {
  required_providers {
    dummycloud = { source = "example/dummycloud" }
  }
}

provider "dummycloud" {
  api_url = "http://127.0.0.1:{{API_PORT}}"
}

resource "dummycloud_server" "imported" {
  name = "imported-01"
  size = "small"
}
