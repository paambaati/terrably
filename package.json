{
  "name": "terrably",
  "private": true,
  "author": {
    "name": "GP",
    "email": "me@httgp.com",
    "url": "https://httgp.com"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "proto:gen": "pnpm --filter terrably run proto:gen",
    "test": "pnpm -r run --if-present test",
    "lint": "pnpm -r run --if-present lint",
    "lint:fix": "pnpm -r run --if-present lint:fix",
    "format": "pnpm -r run --if-present format"
  }
}
