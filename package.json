{
  "name": "terrably",
  "private": true,
  "author": {
    "name": "GP",
    "email": "me@httgp.com",
    "url": "https://httgp.com"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "proto:gen": "pnpm --filter terrably run proto:gen",
    "test": "pnpm -r run test --if-present",
    "lint": "pnpm -r run lint --if-present",
    "lint:fix": "pnpm -r run lint:fix --if-present",
    "format": "pnpm -r run format --if-present"
  }
}
