---
title: Introduction
description: Build Terraform & OpenTofu providers in TypeScript, with full type safety, using Node.js or Bun.
full: true
---

# terrably

`terrably` lets you write [Terraform](https://developer.hashicorp.com/terraform/plugin) and [OpenTofu](https://opentofu.org) providers in **TypeScript** — full type safety, a familiar ecosystem, and using Node.js or Bun.

Providers compile into self-contained binaries. Operators run `terraform apply` exactly as they would with any Go provider — no Node.js or Bun installation required on their machines.

```npm
# Scaffold, implement, build
npx terrably new mycloud
cd terraform-provider-mycloud
npm install
npm run build
# → bin/terraform-provider-mycloud (JS runtime embedded)
```

<Cards>
  <Card href="/docs/getting-started" title="Getting started" icon={<RocketIcon />}>
    Scaffold your first provider and run `terraform plan` in 10 minutes.
  </Card>
  <Card href="/docs/concepts/schema" title="Core concepts" icon={<BookOpenIcon />}>
    Types, schema, resource lifecycle, provider-defined functions.
  </Card>
  <Card href="/docs/development/local-testing" title="Development" icon={<WrenchIcon />}>
    dev_overrides, dev mode, unit tests, debugger, and troubleshooting.
  </Card>
  <Card href="/docs/distribution" title="Distribution" icon={<UploadIcon />}>
    Multi-platform CI, GPG signing, and Terraform Registry publishing.
  </Card>
</Cards>
